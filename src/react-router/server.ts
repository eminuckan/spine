/**
 * React Router server adapter exports.
 *
 * The underlying auth/session/redirect primitives are framework-agnostic,
 * but React Router apps can import from this namespace to make adapter usage
 * explicit and keep room for future framework-specific helpers.
 */

export * from '../server';
