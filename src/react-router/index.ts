/**
 * React Router adapter exports.
 *
 * Today this adapter is a thin alias over the core/client exports.
 * Keeping it as a dedicated entry point lets framework-specific helpers
 * grow here without polluting the framework-agnostic root package.
 */

export * from '../index';
